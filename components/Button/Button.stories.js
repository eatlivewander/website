import React from 'react';
import Button from './Button';

export default {
    title: 'Button',
    component: Button,
};

const Template = () => <Button buttonSize="socialpostcard">Shop This Post</Button>;

export const Default = Template.bind({});
